# 实验六：联机对战 - 接口文档

## 基本信息

- **分支**: `feature/online-multiplayer`
- **服务器模块**: `MyServer/`（Java Library，独立于 Android 模块）
- **Android 包名**: `edu.hitsz`

---

## 环境准备

### 1. 依赖

已在 `app/build.gradle` 中添加：
```groovy
implementation 'com.squareup.okhttp3:okhttp:4.12.0'
implementation 'com.google.code.gson:gson:2.10.1'
```

### 2. 权限

已在 `AndroidManifest.xml` 中添加：
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<application android:usesCleartextTraffic="true" ...>
```

### 3. JDK 配置

项目需要 JDK 21（Android Studio 自带）。
- 能访问 GitHub：Gradle 自动下载，无需额外配置
- 无法访问 GitHub：需设置环境变量 `JAVA_HOME` 指向 Android Studio 的 `jbr` 目录，例如：
  ```
  JAVA_HOME=E:\Android\Android Studio\jbr
  ```

---

## 服务器启动

### 终端方式

```bash
./gradlew :MyServer:run
# 看到 "HTTP server started on http://localhost:8080" 即成功
# Ctrl+C 停止
```

### Android Studio 方式

```
Run → Edit Configurations → + → Application
  Name: MainServer
  Main class: edu.hitsz.server.MainServer
  Module classpath: MyServer
```

---

## HTTP REST API（排行榜）

- **地址**: `http://<host>:8080/api/records`
- **模拟器访问宿主机**: `http://10.0.2.2:8080/api/records`

### GET — 查询排行榜

```
GET /api/records?difficulty=normal
```

响应（JSON 数组，按分数降序）：
```json
[
  {
    "playerName": "Player_2847",
    "score": 1600,
    "difficulty": "normal",
    "timestamp": 1777993997049
  }
]
```

### POST — 上传一条记录

```
POST /api/records
Content-Type: application/json

{
  "playerName": "Player_2847",
  "score": 1600,
  "difficulty": "normal"
}
```

响应：
```json
{"success": true}
```

### DELETE — 删除一条记录

```
DELETE /api/records?difficulty=normal&id=0
```

响应：
```json
{"success": true}
```

---

## Socket 实时通信协议

- **地址**: `<host>:9999`
- **模拟器**: `10.0.2.2:9999`
- **格式**: 纯文本行协议，每行以换行符结束，`:` 分隔命令和参数

### 客户端 → 服务器

| 消息 | 说明 | 时机 |
|------|------|------|
| `JOIN:房间号` | 加入指定房间 | 连接后立即发送 |
| `SCORE:分数` | 上报当前得分 | 游戏中得分变化时 |
| `GAMEOVER:最终分数` | 通知死亡 | 英雄机 HP 归零时 |

### 服务器 → 客户端

| 消息 | 说明 | 触发条件 |
|------|------|----------|
| `WAITING` | 等待对手中 | 第一个进入房间的玩家 |
| `START` | 双方就位，开始对战 | 第二个玩家加入房间时，双方均收到 |
| `OPPONENT:分数` | 对手当前得分 | 对手发送 SCORE 时转发 |
| `OPPONENT_GAMEOVER:分数` | 对手已死亡 | 对手发送 GAMEOVER 时转发 |
| `OPPONENT_DISCONNECT` | 对手断线 | 对手连接断开时 |

### 典型流程

```
客户端A → 服务器:  JOIN:room1
服务器 → 客户端A:  WAITING

客户端B → 服务器:  JOIN:room1
服务器 → 客户端B:  START
服务器 → 客户端A:  START

客户端A → 服务器:  SCORE:100
服务器 → 客户端B:  OPPONENT:100

客户端B → 服务器:  GAMEOVER:960
服务器 → 客户端A:  OPPONENT_GAMEOVER:960

客户端A → 服务器:  GAMEOVER:1600
服务器 → 客户端B:  OPPONENT_GAMEOVER:1600   (若B还在线)
```

---

## 项目文件结构

```
MyServer/                          # 服务器模块（Java Library）
└── src/main/java/edu/hitsz/server/
    ├── MainServer.java            # 入口：HTTP 8080 + Socket 9999
    ├── GameRoomManager.java       # 房间管理（单例 + ConcurrentHashMap）
    ├── http/
    │   └── RecordHandler.java     # 排行榜 REST API 处理器
    ├── socket/
    │   └── GameSocketHandler.java # Socket 连接处理器（Runnable）
    └── model/
        └── GameRecord.java        # 服务器端数据模型

app/                               # Android 客户端
└── src/main/java/edu/hitsz/
    ├── network/
    │   ├── ServerConfig.java      # 服务器地址常量
    │   └── SocketClient.java      # Socket 客户端封装
    ├── dao/
    │   ├── GameRecordDao.java      # 排行榜 DAO 接口（已有）
    │   ├── GameRecordDaoImpl.java  # 本地文件存储（已有）
    │   ├── GameRecordDaoCloud.java # 🆕 云端存储（OkHttp）
    │   └── GameRecord.java        # 数据模型（已有）
    ├── manager/
    │   ├── GameManager.java        # 游戏管理器接口（已有，新增 2 方法）
    │   ├── SinglePlayerManager.java # 单机模式（已有）
    │   └── OnlineGameManager.java  # 🆕 联机模式
    ├── RoomActivity.java           # 🆕 房间号输入界面
    ├── MainActivity.java           # 改动：联机模式初始化
    ├── DifficultyActivity.java     # 改动：联机路由到 RoomActivity
    └── application/
        └── Game.java               # 改动：对手 HUD + 云端保存
```

---

## 测试步骤

1. 启动服务器（见上方"服务器启动"）
2. 打开两个安卓模拟器
3. 两台均：App → 联机模式 → 选择难度 → 输入同一房间号 → 加入
4. 正常对战，观察对方 HUD 右上角对手分数
5. 游戏结束后进入排行榜 → 点击"云端"标签 → 查看服务器记录

### 单元测试（curl）

```bash
# 添加
curl -X POST http://localhost:8080/api/records \
  -H "Content-Type: application/json" \
  -d '{"playerName":"Alice","score":500,"difficulty":"normal"}'

# 查询
curl http://localhost:8080/api/records?difficulty=normal

# 删除
curl -X DELETE "http://localhost:8080/api/records?difficulty=normal&id=0"
```
